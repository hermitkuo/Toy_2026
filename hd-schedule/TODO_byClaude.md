# TODO_byClaude — 後續建議清單

> 由 Claude 依對專案的觀察提出，供排序參考。分優先序，未經使用者確認；`TODO.md` 才是既定計劃。
> 產生日期：2026-07-05

## P0 — 資料隱私（原始目標，仍未動）

- [ ] **完成 Apps Script `doGet()` 代理**，讓 `SHEET_ID`／`GID` 不再出現在前端原始碼。
      現況：`index.html` 直接以 gviz JSONP 讀公開試算表 → **醫師姓名等同全公開**（任何人看 Network 分頁即得整份班表）。
      這是 `TODO.md` 最初立項的核心目標，但四頁功能都做完了、它反而一直沒收尾。建議重新評估是否為當前優先。
- [ ] 決策：只是「藏 ID」還是「連資料都不外流」？後者需對端點加身分驗證（通關碼／網域限制）。
      若醫師姓名視為個資，這題不宜無限期擱置——先跟使用者確認風險胃納。

## P1 — 資料串接架構（目前是斷的）

- [ ] **`leave.html` / `preference.html` → `schedule.html` 之間目前靠 `localStorage`，跨裝置不通。**
      醫師在自己手機填請假，資料留在醫師的瀏覽器；排班者在自己的電腦開 `schedule.html` 讀不到。
      這不只是「還沒接後端」，是現行設計無法運作的環節。建議：請假/偏好一律經 Apps Script 寫回試算表分頁，
      `schedule.html` 改讀該分頁（而非 localStorage），localStorage 僅當本機草稿快取。
- [ ] 統一「請假/偏好」的權威來源為試算表分頁，並在三頁定義一致的欄位契約（doctor／month／時段／note）。

## P2 — 發布鏈路啟用（半成品，待部署）

- [ ] 部署 `publish_webapp.gs` 成 Web App，記下 `/exec` 網址＋通關碼，填入 `schedule.html` 發布區。
- [ ] 於 `index.html` 填 `DRAFT_GID`（「自動排班草稿」分頁 gid）啟用閱覽頁 B 模式；實測草稿→閱覽全鏈路。
- [ ] 端到端驗證一次：排班者發布 → 醫師端 `index.html` 看到草稿 → 換月/篩選/列印皆正常。

## P3 — 排班演算法品質

- [ ] S6 偏好納入自動排班（目前偏好頁收集了但排班未用）。
- [ ] 掛名輪替：目前偏集中在少數人，加入平衡機制（可參考 `setup_settings_sheet.gs` 的醫師配額分頁）。
- [ ] 遞補由「僅建議」改為「可一鍵自動套用」，並保留人工否決。
- [ ] **連寫名自動拆分（如「翔郭」）依賴寫死的單字醫師集** → 新醫師加入時會靜默拆錯。
      建議把該名單集中成一處設定，或改由試算表提供，避免散落在 `index.html` 統計模組內。

## P4 — 程式健康度與維運

- [ ] `index.html` 已達 ~2485 行單檔；gid 等常數散落多處（現行/歷史/假日/草稿）。
      建議把所有 Sheet 設定集中到檔頭單一 config 物件，降低改 gid 時漏改的風險。
- [ ] 補一份輕量 smoke 驗證清單（四頁 × 換月/篩選/列印/暗色）或最小 Playwright 腳本，
      取代每次全手動驗證；目前完全依賴人工回歸。
- [ ] `TODO.md.bak` 仍在工作區（git status 顯示為 modified）。決定：刪除，或加入 `.gitignore`。
- [ ] `TODO.md` 底部「背景／目標／工作項目」區塊是最初的代理化 brief，和上方已完成計劃並列易混淆。
      建議把「未完成的原始目標」上移或獨立成一節，讓「還沒做的事」一眼可見（本清單 P0 已對應）。

## P5 — 小體驗

- [ ] `找統計` 是否也需要列印/匯出？（目前只有 `找班表` 有 🖨。）視行政實際需求再決定，勿過度設計。
- [ ] 確認四頁在「試算表暫時讀不到／草稿為空／跨年邊界」時的空狀態與錯誤提示一致且友善。
